# Copiar este archivo como .env (solo para desarrollo local con `vercel dev`)
# En producción, esta variable se configura en Vercel: Project → Settings → Environment Variables
# Nunca subir el archivo .env real a GitHub (ya está en .gitignore).

GROQ_API_KEY=tu_api_key_de_groq_aqui
